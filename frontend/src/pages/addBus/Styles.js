import { makeStyles } from "@material-ui/core";


export default makeStyles((theme)=>({
  branchAdd:{
    width:300,
    float:'right',
    margin:'1rem auto',
    
  },
  icon:{
    fontSize:60,
    color:theme.palette.primary
  }
}))